<?php
/**
 * Instagram feed — DISABLED.
 *
 * The original Imaginal version used the v1 Instagram API endpoint
 * (api.instagram.com/v1) which was permanently shut down by Meta in 2020.
 * Calling it caused silent fatal errors and broken pages.
 *
 * If you want the Instagram feed back, install a maintained plugin such as
 * "Smash Balloon Social Photo Feed" — it handles Instagram OAuth + caching
 * properly and outputs a shortcode you can drop into the footer widget.
 */
return;
